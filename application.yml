server:
  port: 8192
spring:
  profiles:
    active: native
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GautamChaursia/spring-cloud-config-store
          search-paths:
          - '*service'
